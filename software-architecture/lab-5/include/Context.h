#pragma once

#include <string>

// Context — глобальная для интерпретатора информация. В нашей задаче это
// просто корректируемый текст: каждое правило читает его, применяет
// преобразование и возвращает обратно в этот же буфер.
struct Context {
    std::string text;
};

#1
Полнота по Тьюрингу — характеристика исполнителя (множества вычисляющих элементов) в теории вычислимости,
означающая возможность реализовать на нём любую вычислимую функцию

#2
Для вычисления констант
Для работы с интервалами времени(для вычисления результирующих единиц измерения)

#3
Нетиповые параметры шаблона
Специализации шаблонов
Рекурсивное инстанцирование
SFINAE и enable_if
constexpr (переменные, функции, if )

#4
В c++ 11 constexpr может сосотоять только из одного return
Неудобно писать шаблоны
Ограничение глубины рекурсии
Ограничение на кол-во выполненных действий

#5
Часть операций производится на этапе компиляции, часть в runtime.
Например при работе с промежутками времени (в библиотеке chrono)
Вычисление результирующих единиц измерения происходит на этапе компиляции, а
сложение этих промежутков в runtime